<div class="card">
  <div class="card-body">
    <h1 class="m-5">Dashboard</h1>
    <p>
      You must be authenticated to see this page so if you're seeing it then
      everything worked as expected 🎉
    </p>
    <h2>Current User</h2>
    <p>Id: <%= current_user.uid %></p>
    <p>Name: <%= current_user.name %></p>
    <p>Username: <%= current_user.username %></p>
    <p>Email: <%= current_user.email %></p>
    <p><a href="/logout" class="btn btn-danger">Logout system and Keycloak</a></p>
    <p>
      <%= link_to "My Posts", posts_path, class: "btn btn-primary" %>
    </p>
  </div>
</div>